// src/app/dashboard/journal/new/editor/SlashCommands.tsx
// ─────────────────────────────────────────────────────
// Minimal stub — compiles immediately. Replace with a full
// slash-command Extension when you're ready to build that feature.
// The FloatingMenu in TiptapEditor already covers the same use-case
// for now (appears on any empty line, same block types).

export default function SlashCommands() {
  return null
}